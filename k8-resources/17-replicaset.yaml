apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  labels:
    name: rs-demo
    project: roboshop

spec:
  replicas: 3
  selector:
    matchLabels:
        name: rs-demo
        project: roboshop
  template:
    metadata:
      labels:  
        name: rs-demo
        project: roboshop
    spec:
      containers: 
      - name: nginx
        image: nginx   