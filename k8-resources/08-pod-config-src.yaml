apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-src
  labels:
    name: label
  annotations:
    name: annotations
  #namespace: roboshop

spec:
  containers:
    - name: nginx-pod-src
      image: nginx
      envFrom:
        - configMapRef:
            name: configmap-test