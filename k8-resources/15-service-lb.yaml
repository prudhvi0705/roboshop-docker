apiVersion: v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    course: devk8s
    key: learn
    purpose: LoadBalancer

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      NodePort: 30007