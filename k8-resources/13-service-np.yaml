apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
spec:
  type: nodeport
  selector:
    project: roboshop
    course: devk8s
    key: learn
    purpose: nodeport

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80