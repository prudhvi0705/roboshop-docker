apiVersion: v1
kind: Pod
metadata:
  name: nginx
  description: nginx pod
  labels:
    name: nginx
    type: learn

spec:
  containers: 
  - name: nginx
  - image: nginx
    ports:
  - container ports: 80
